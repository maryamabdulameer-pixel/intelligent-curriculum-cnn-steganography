# Scripts

`train_v93_soft_hard_sample_finetune.py` is the V9.3 soft hard-sample fine-tuning code. It starts from the best V9.1 checkpoint and uses hard-sample indices mined by V9.2 with soft weighting.
